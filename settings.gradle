// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
pluginManagement {
    plugins {
        id 'jacoco'
        id 'application'
        id 'com.github.johnrengelman.shadow' version '7.1.2'
        id 'io.specgen.gradle' version '2.1.1720'
        id 'io.micronaut.minimal.application' version '3.4.1'
        id 'dev.medly.repos' version '0.0.11'
        id 'dev.medly.kotlin' version '0.0.11'
        id 'dev.medly.detekt' version '0.0.11'
        id 'com.github.node-gradle.node' version '3.2.1'
    }
    repositories {
        exclusiveContent {
            forRepository {
                maven {
                    name = "Medly Maven Repository"
                    url = "https://maven.pkg.github.com/medlypharmacy/maven-artifacts"
                    credentials {
                        if (System.getenv("CI")) {
                            username = System.getenv("GITHUB_ACTOR")
                            password = System.getenv("MEDLY_GPR_READ")
                        } else {
                            username = medlyGithubUsername
                            password = medlyGithubToken
                        }
                    }
                }
            }
            filter {
                includeGroupByRegex 'dev\\.medly\\.[\\w-]*' // plugin marker group id are plugin's names
                includeGroup "dev.medly" // plugin artifact's group id
            }
        }
    
        gradlePluginPortal();
    }
}
rootProject.name = "test-project"

include(':api')
project(':api').setBuildFileName('api.gradle')

# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: Upgrade Project
on:
  workflow_dispatch: {}
  schedule:
    - cron: 30 12 * * 1
jobs:
  upgrade:
    uses: medlypharmacy/reusable-workflows/.github/workflows/upgrade-kotlin-projen.yml@main
    with:
      defaultBranch: ${{ github.event.repository.default_branch }}
    secrets: inherit
